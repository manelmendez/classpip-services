sudo: required
language: ruby

services:
  - docker

script:
  - npm install
  - npm test
  - docker build -t classpip/classpip-services .

deploy:
  provider: script
  script: ./deploy.sh
  on:
    branch: master
